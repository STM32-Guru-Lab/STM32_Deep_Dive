# OpenOCD config for STM32F103C6 + ST-LINK (SWD)
source [find interface/stlink.cfg]
transport select hla_swd

source [find target/stm32f1x.cfg]

# Conservative speed for stable bring-up
adapter speed 1000

# Use software reset path to work even if NRST is not wired
reset_config none
